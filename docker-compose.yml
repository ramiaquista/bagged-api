services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bagged
      POSTGRES_PASSWORD: bagged
      POSTGRES_DB: bagged
    ports:
      - "5432:5432"
    volumes:
      - bagged_db_data:/var/lib/postgresql/data

volumes:
  bagged_db_data:
